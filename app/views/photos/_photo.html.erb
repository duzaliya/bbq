<div class="event-photo mb-2">
  <%= link_to image_tag(photo.photo.variant(resize_to_fit: [100, 100])), url_for(photo.photo) %>

  <%= t('photos.added') %> <%= render partial: 'users/user', object: photo.user %>
</div>

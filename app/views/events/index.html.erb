<h1><%= t('events.index') %></h1>

<div class="my-4">
  <%= link_to t('events.create'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-2">
  <%= render partial: 'events/event', collection: @events %>
</div>
